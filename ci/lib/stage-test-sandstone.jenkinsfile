stage('test') {
    if (env.no_cpu.toInteger() > 16)
    {
        try{
            sh '''
                cd CI-Examples/sd-test
                if [ "${EDMM}" ]
                then
                    sed -i 's/sgx.enclave_size = "16G"/#sgx.enclave_size = "16G"/g' sd-test.manifest.template
                    sed -i 's/sd_150_test_16gb.list/sd_150_test_edmm.list/g' Makefile
                    make && start-sd-test 2>&1 | tee OUTPUT_EDMM.txt
                else
                    make && make start-sd-test 2>&1 | tee OUTPUT_16GB.txt
                    sed -i 's/sd_150_test_16gb.list/sd_150_test_32gb.list/g' Makefile
                    sed -i 's/sgx.enclave_size = "16G"/sgx.enclave_size = "32G"/g' sd-test.manifest.template
                    make clean && make
                    make start-sd-test 2>&1 | tee OUTPUT_32GB.txt
                fi
            '''
        } catch (Exception e) {
            env.build_ok = false
            sh 'echo "sdtest workload Failed"'
        }
    }   
}

stage('verification') {            
    try {
        timeout(time: 2, unit: 'MINUTES') {
            sh 'python3 -m pytest -v -m sdtest --junit-xml sdtest.xml test_workloads.py'
        }
    } catch (Exception e){
        env.build_ok = true
        sh 'echo "sdtest parser failed"'
    } finally {
        junit 'sdtest.xml'
    }
}
