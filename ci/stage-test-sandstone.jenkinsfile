stage('test') {
    timeout(time: 90, unit: 'MINUTES') {
        sh '''
            cd Examples/sandstone-50-bin
            if test -n "$SGX"
            then
                make SGX=1 
                SGX=1 make start-sandstone
            fi
        '''
    }
}
            
